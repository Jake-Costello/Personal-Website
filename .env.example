# Public URL only. Never place API keys in VITE_ variables.
# Leave empty to run the explorer in its clearly labeled illustrative mode.
VITE_API_BASE_URL=
# Local development uses /. GitHub Actions sets this to /Personal-Website/.
VITE_BASE_PATH=/
